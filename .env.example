# Copy to .env or export in your shell. Only needed for live review-agent runs
# (eval/harness.py --live and src/review_agent). Everything else in the repo,
# including replaying the committed eval results, works without it.
ANTHROPIC_API_KEY=sk-ant-your-key-here
